import QtQuick 2.5

import WGControls.Canvas 2.0 as Can

// !!! This file will be deleted in the near future. It is here only as a reminder to update old controls.

Can.WGSelectionArea{
    Component.onCompleted: {
        console.log("***DEPRECATED*** - " + this + " <=== this file is in an old location")
    }
}
