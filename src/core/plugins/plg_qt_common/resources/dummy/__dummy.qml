//This is dummy file to fix the windeployqt.exe issue
//The only purpose for this file is just let windeployqt knows that it should
//deploy the QtQuick/PrivateWidgets folder.

import QtQuick 2.2
import QtQuick.PrivateWidgets 1.0
import QtCanvas3D 1.0

QtColorDialog { }
